//Null y Undefined ==> cada uno tiene solo un valor

let variable1 = null; // null representa algo que no tiene valor (vacio)
let variable2 = undefined; // undefined representa que algo no ha sido definido (no existe)

let variable3; // por defecto es undefined

// Comparacion de null y undefined

null === undefined // false
undefined === undefined // true
null === null // true
